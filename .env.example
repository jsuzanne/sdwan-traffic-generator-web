# SD-WAN Traffic Generator - Environment Configuration

# Web UI Port (default: 8080)
# Change this if port 8080 is already in use
WEB_UI_PORT=8080

# JWT Secret for authentication
# IMPORTANT: Change this in production!
JWT_SECRET=change-this-secret-in-production

# Traffic Generation Settings
# Time to sleep between requests (in seconds)
SLEEP_BETWEEN_REQUESTS=1

# Configuration and Log Directories
# Usually don't need to change these
CONFIG_DIR=/app/config
LOG_DIR=/var/log/sdwan-traffic-gen

# Node Environment
NODE_ENV=production
